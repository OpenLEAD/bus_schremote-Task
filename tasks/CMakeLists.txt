# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(bus_schremoteTaskLib)
ADD_LIBRARY(${BUS_SCHREMOTE_TASKLIB_NAME} SHARED 
    ${BUS_SCHREMOTE_TASKLIB_SOURCES})
add_dependencies(${BUS_SCHREMOTE_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${BUS_SCHREMOTE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${BUS_SCHREMOTE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${BUS_SCHREMOTE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${BUS_SCHREMOTE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${BUS_SCHREMOTE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${BUS_SCHREMOTE_TASKLIB_HEADERS}
    DESTINATION include/orocos/bus_schremote)

